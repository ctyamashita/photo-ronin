<div class="map" data-map-toggle-target="map">
  <div style="width: 100%; height: 90vh; margin: 0; padding: 0"
    data-controller="mapbox"
    data-mapbox-markers-value="<%= marker.to_json %>"
    data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
