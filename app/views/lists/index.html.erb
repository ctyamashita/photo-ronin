<div class="container col-11 mt-5">
  <h2 class="mb-3 bold">All lists</h2>
  <div class="grid">
    <% if @lists.any? %>
      <% @lists.each do |list|%>
        <%= render "list_card", list: list %>
      <% end %>
    <% else %>
      No lists.
    <% end %>
  </div>
</div>
